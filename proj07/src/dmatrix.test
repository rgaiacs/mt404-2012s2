Test dcopy_matrix

    >>> import dmatrix
    >>> A = [[1, 0], [0, 1]]
    >>> A_copy = dmatrix.dcopy_matrix(A, 2, 2)
    >>> A_copy
    array([[ 1.,  0.],
           [ 0.,  1.]])

Test dshow_matrix is not possible beacuse "Python I/O will not catch I/O from
Fortran" (see http://osdir.com/ml/python.scientific.user/2003-10/msg00034.html)
